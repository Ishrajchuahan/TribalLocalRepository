using System;
using System.Collections.Generic;

using System.Web;
using System.Web.UI;
using System.Web.UI.WebControls;

public partial class mis_Registration_GenerateSalesInvoice : System.Web.UI.Page
{
    protected void Page_Load(object sender, EventArgs e)
    {
        //string selectedItems = string.Join(",", lstSchoolMedium.Items.Cast<ListItem>().Where(li => li.Selected).Select(li => li.Value));        //string selectedItems = string.Join(",", lstSchoolMedium.Items.Cast<ListItem>().Where(li => li.Selected).Select(li => li.Value));

    }
}